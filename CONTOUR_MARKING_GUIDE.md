# 滑块验证码轮廓标记功能使用指南

## 功能概述

这个功能利用大模型的图像理解能力，精确识别滑块验证码中的滑块和缺口位置，并在图像上用长方形轮廓标出它们的边界。

## 主要特性

- 🎯 **精确定位**: 利用大模型精确识别滑块和缺口的位置坐标
- 🖼️ **可视化标记**: 在原图上用彩色边框清晰标出轮廓
- 📊 **详细信息**: 显示尺寸、中心点、滑动距离等详细数据
- 🔧 **智能解析**: 多种解析方法确保识别准确性
- 💫 **直观显示**: 双边框设计，信息丰富且易于理解

## 使用方法

### 方法一：使用测试脚本（推荐）

```bash
python test_contour_marking.py
```

这个脚本会：
1. 自动连接Android设备
2. 截取当前屏幕
3. 使用大模型分析滑块验证码
4. 标记轮廓并保存结果图像
5. 可选择是否执行实际的滑块操作

### 方法二：在代码中调用

```python
from scripts.captcha_agent import CaptchaAgent

# 初始化Agent
agent = CaptchaAgent()
# 设置模型和控制器...

# 调用轮廓标记功能
marked_image_path = agent.mark_slider_contours(screenshot_path)
```

## 标记说明

生成的标记图像包含以下元素：

### 颜色标记
- **🟢 绿色双边框**: 滑块的轮廓边界
- **🔴 红色双边框**: 缺口的轮廓边界
- **🟢 绿色圆点**: 滑块的中心位置
- **🔴 红色圆点**: 缺口的中心位置
- **🔵 蓝色箭头**: 从滑块到缺口的滑动路径

### 文字信息
- **滑块标签**: 显示滑块尺寸（如：Slider 50x50）
- **缺口标签**: 显示缺口尺寸（如：Gap 45x45）
- **距离信息**: 显示滑动距离（如：Distance: 320px）
- **坐标详情**: 在图像底部显示精确的坐标信息

## 大模型提示词格式

系统使用专门设计的提示词来指导大模型分析：

```
**滑块轮廓信息：**
- 滑块边界坐标：左上角(x1,y1), 右下角(x2,y2)
- 滑块中心坐标：(cx,cy)
- 滑块宽度：xx像素
- 滑块高度：xx像素

**缺口轮廓信息：**
- 缺口边界坐标：左上角(x1,y1), 右下角(x2,y2)
- 缺口中心坐标：(cx,cy)
- 缺口宽度：xx像素
- 缺口高度：xx像素
```

## 输出文件

标记后的图像保存在：`./temp/marked_slider_contours.png`

## 错误处理

系统具备多层错误处理机制：

1. **主要解析**: 使用正则表达式精确匹配大模型输出
2. **智能提取**: 当主要解析失败时，使用启发式方法提取坐标
3. **默认值**: 当所有解析都失败时，使用经验默认值
4. **数据验证**: 检查解析结果的完整性和合理性

## 配置要求

### 必需配置
- 正确配置的LLM模型（OpenAI或Qwen）
- 连接的Android设备
- 有效的API密钥

### 推荐设置
- 模型温度: 0.1-0.3（确保输出稳定）
- 最大令牌数: 2000+（确保完整响应）

## 应用场景

1. **验证码调试**: 查看滑块和缺口的精确位置
2. **算法优化**: 验证滑块检测算法的准确性
3. **数据分析**: 收集不同验证码的尺寸和位置数据
4. **测试验证**: 检查滑块操作的精确度

## 性能特点

- **准确性**: 基于大模型的语义理解，比传统图像处理更准确
- **适应性**: 能够处理各种风格和类型的滑块验证码
- **稳定性**: 多重解析机制确保在各种情况下都能工作
- **可视化**: 直观的标记让结果一目了然

## 故障排除

### 常见问题

1. **无法识别滑块**
   - 检查当前页面是否包含滑块验证码
   - 确认大模型API正常工作

2. **坐标不准确**
   - 可能是大模型理解偏差，可以重试几次
   - 检查截图质量是否清晰

3. **程序崩溃**
   - 检查网络连接
   - 验证API密钥有效性
   - 确认设备连接正常

### 调试模式

程序会输出详细的调试信息：
- 大模型的完整响应
- 解析过程的详细步骤
- 最终提取的坐标数据

## 扩展功能

未来可能的增强：
- 支持更多验证码类型
- 添加轮廓精度评估
- 集成机器学习验证
- 支持批量处理

---

*如有问题，请检查配置文件和网络连接，或查看程序输出的详细错误信息。* 